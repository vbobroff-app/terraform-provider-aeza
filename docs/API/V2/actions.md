# **Управление услугами (Services Control)**
🎯 Операции управления состоянием услуг (включение, выключение, перезагрузка)

### 📋 **Основная информация**
Управление услугами - операции изменения состояния питания и перезагрузки услуг через единый endpoint с параметризацией действия.

### 🔧 **API Endpoint**
#### V2 API

```bash
curl -X 'POST' \
  'https://my.aeza.net/api/v2/services/{id}/ctl/{action}' \
  -H 'accept: application/json' \
  -H 'X-API-KEY: yourAPIkey'
```

#### 📥 **Path Parameters**
| Параметр | Тип | Обязательный | Описание | Допустимые значения |
|----------|-----|-------------|-----------|-------------------|
| `id` | int64 | ✅ | ID услуги | Положительное целое число |
| `action` | string | ✅ | Действие управления | `"suspend"`, `"resume"`, `"restart"` |

### 📤 **Response Structure**
Успешный ответ - пустое тело с HTTP статусом 201 Created

#### 🚨 **Коды ответа**
| Код | Описание | Тело ответа |
|-----|-----------|-------------|
| `201` | Успешное выполнение операции | Пустое |
| `400` | Неверные параметры или недопустимое состояние | JSON с описанием ошибки |
| `401` | Ошибка аутентификации | JSON с описанием ошибки |
| `404` | Услуга не найдена | JSON с описанием ошибки |
| `409` | Конфликт состояния услуги | JSON с описанием ошибки |

### **Доступные действия**
`suspend` - **Выключение услуги**

**Описание:** Приостановка работы услуги (soft power off)

#### Условия выполнения:

- Услуга должна быть в статусе `"active"`

- Не должно быть активных задач

#### Пример:

```bash
curl -X 'POST' \
  'https://my.aeza.net/api/v2/services/1577409/ctl/suspend' \
  -H 'X-API-KEY: yourAPIkey'
```
`resume` - **Включение услуги**

**Описание:** Возобновление работы приостановленной услуги

#### Условия выполнения:

- Услуга должна быть в статусе `"suspended"`

- Не должно быть активных задач

#### Пример:

```bash
curl -X 'POST' \
  'https://my.aeza.net/api/v2/services/1577409/ctl/resume' \
  -H 'X-API-KEY: yourAPIkey'
```

`restart` - **Выключение услуги**

**Описание:** Перезагрузка работающей услуги

#### Условия выполнения:

- Услуга должна быть в статусе `"active"`

- Не должно быть активных задач

#### Пример:

```bash
curl -X 'POST' \
  'https://my.aeza.net/api/v2/services/1577409/ctl/restart' \
  -H 'X-API-KEY: yourAPIkey'
```
### 📝 **Примеры ошибок**
#### Ошибка 400 - Недопустимое состояние
```json
{
  "statusCode": 400,
  "message": "Service is in an invalid status",
  "error": "service.invalid_status",
  "timestamp": "2025-11-29T12:33:02.487Z",
  "path": "/services/1577409/ctl/resume",
  "payload": {
    "id": "active",
    "allow": ["suspended"],
    "current": "active"
  },
  "requestId": "913f0141-014a-4120-b06d-f0848f854913"
}
```
#### Ошибка 404 - Услуга не найдена
```json
{
  "statusCode": 404,
  "message": "Service not found",
  "error": "service.not_found", 
  "timestamp": "2025-11-29T12:35:12.123Z",
  "path": "/services/9999999/ctl/restart",
  "requestId": "a1b2c3d4-5678-90ef-ghij-klmnopqrstuv"
}
```
#### Ошибка 409 - Конфликт состояния
```json
{
  "statusCode": 409,
  "message": "Service has active task",
  "error": "service.active_task",
  "timestamp": "2025-11-29T12:36:45.678Z",
  "path": "/services/1577409/ctl/suspend",
  "payload": {
    "currentTask": "reboot_in_progress"
  },
  "requestId": "wxyz1234-5678-90ab-cdef-ghijklmnopqr"
}
```
### ⚡ **Rate Limiting**
- Лимит: 10 запросов в минуту на endpoint

- Заголовки ответа:

    - X-Ratelimit-Limit: общий лимит

    - X-Ratelimit-Remaining: оставшиеся запросы

    - X-Ratelimit-Reset: время сброса лимита (секунды)

#### 🔄 **Состояния услуги**
| Состояние | Описание | Допустимые действия |
|-----------|-----------|-------------------|
| `active` | Услуга активна и работает | `suspend`, `restart` |
| `suspended` | Услуга приостановлена | `resume` |
| `activation_wait` | Ожидание активации | Нет |
| `deleted` | Услуга удалена | Нет |

### 📊 **Пример workflow**
Последовательное управление состоянием
```bash
# 1. Проверяем текущий статус
curl -X 'GET' 'https://my.aeza.net/api/services/1577409' -H 'X-API-KEY: yourAPIkey'

# 2. Выключаем услугу (если активна)
curl -X 'POST' 'https://my.aeza.net/api/v2/services/1577409/ctl/suspend' -H 'X-API-KEY: yourAPIkey'

# 3. Включаем услугу (если выключена)  
curl -X 'POST' 'https://my.aeza.net/api/v2/services/1577409/ctl/resume' -H 'X-API-KEY: yourAPIkey'

# 4. Перезагружаем услугу (если активна)
curl -X 'POST' 'https://my.aeza.net/api/v2/services/1577409/ctl/restart' -H 'X-API-KEY: yourAPIkey'
```

### ⚠️ **Особенности**
- Все операции идемпотентны - повторный вызов с тем же состоянием не выполняется (выдает ошибку) 

- Ответ 201 Created с пустым телом при успехе

- Детальные ошибки с указанием допустимых состояний

- Поддержка rate limiting для предотвращения злоупотреблений

Использует [V2 API](../../API/api.md) с улучшенной обработкой ошибок